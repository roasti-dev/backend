# yaml-language-server: $schema=https://raw.githubusercontent.com/OAI/OpenAPI-Specification/refs/heads/main/_archive_/schemas/v3.0/schema.yaml
openapi: 3.0.0
info:
  version: 0.0.1
  title: Roasti App
paths:
  /health:
    get:
      responses:
        '200':
          description: OK
          content:
            text/plain:
              schema:
                type: string
                example: "OK"
  /api/v1/recipes:
    get:
      summary: List recipes
      parameters:
        - $ref: '#/components/parameters/UserIDHeader'
        - $ref: '#/components/parameters/AuthorIDParam'
        - $ref: '#/components/parameters/BrewMethodParam'
        - $ref: '#/components/parameters/DifficultyParam'
        - $ref: '#/components/parameters/LimitParam'
        - $ref: '#/components/parameters/PageParam'
      responses:
        '200':
          description: Paginated list of recipes
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedRecipes'
        '400':
          description: Bad request
        '500':
          description: Internal server error

    post:
      summary: Create recipe
      parameters:
        - $ref: '#/components/parameters/UserIDHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateRecipeRequest'
      responses:
        '201':
          description: Recipe created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateRecipeResponse'
        '400':
          description: Bad request

  /api/v1/recipes/{recipe_id}:
    delete:
      summary: Delete recipe
      parameters:
        - $ref: '#/components/parameters/UserIDHeader'
        - in: path
          name: recipe_id
          required: true
          schema:
            type: string
      responses:
        '204':
          description: Deleted
        '400':
          description: Bad request

    patch:
      summary: Update recipe
      parameters:
        - $ref: '#/components/parameters/UserIDHeader'
        - name: recipe_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchRecipeRequest'
      responses:
        '200':
          description: Recipe updated
          content:
            application/json:
              schema:
                $ref: './models.yaml#/components/schemas/Recipe'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: './models.yaml#/components/schemas/ApiErrorResponse'
        '403':
          description: Not allowed (not author)
          content:
            application/json:
              schema:
                $ref: './models.yaml#/components/schemas/ApiErrorResponse'
        '404':
          description: Recipe not found
          content:
            application/json:
              schema:
                $ref: './models.yaml#/components/schemas/ApiErrorResponse'

components:
  parameters:
    UserIDHeader:
      name: X-User-Id
      in: header
      description: User ID
      schema:
        type: string
        default: test-user
      required: true
    AuthorIDParam:
      name: author_id
      in: query
      description: ID of the author
      required: false
      schema:
        type: string

    BrewMethodParam:
      name: brew_method
      in: query
      description: Filter by brew method
      required: false
      schema:
        $ref: './models.yaml#/components/schemas/BrewMethod'

    DifficultyParam:
      name: difficulty
      in: query
      description: Filter by difficulty
      required: false
      schema:
        $ref: './models.yaml#/components/schemas/Difficulty'

    LimitParam:
      name: limit
      in: query
      description: Maximum number of recipes to return
      required: false
      schema:
        type: integer
        default: 20

    PageParam:
      name: page
      in: query
      description: Page of recipes
      required: false
      schema:
        type: integer
        default: 1

  schemas:
    PaginatedResult:
      type: object
      description: Generic paginated response
      properties:
        items:
          type: array
          items: {}
          description: List of items on this page
        page:
          type: integer
          format: uint64
          description: Current page number
        limit:
          type: integer
          format: uint64
          description: Maximum number of items per page
        total_count:
          type: integer
          format: int64
          description: Total number of items available
      required:
        - items
        - page
        - limit
        - total_count
    PaginatedRecipes:
      allOf:
        - $ref: '#/components/schemas/PaginatedResult'
        - type: object
          properties:
            items:
              type: array
              items:
                $ref: './models.yaml#/components/schemas/Recipe'

    CreateRecipeRequest:
      type: object
      additionalProperties: false
      required:
        - title
        - description
        - brew_method
        - difficulty
        - steps
      properties:
        title:
          type: string
        description:
          type: string
        image_url:
          type: string
          nullable: true
          description: Optional image illustrating the recipe
        brew_method:
          $ref: './models.yaml#/components/schemas/BrewMethod'
        difficulty:
          $ref: './models.yaml#/components/schemas/Difficulty'
        roast_level:
          $ref: './models.yaml#/components/schemas/RoastLevel'
        beans:
          type: string
        public:
          type: boolean
          default: false
          description: Whether this recipe is publicly visible
        steps:
          type: array
          items:
            $ref: './models.yaml#/components/schemas/BrewStep'

    PatchRecipeRequest:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        image_url:
          type: string
          nullable: true
        brew_method:
          $ref: './models.yaml#/components/schemas/BrewMethod'
        difficulty:
          $ref: './models.yaml#/components/schemas/Difficulty'
        roast_level:
          $ref: './models.yaml#/components/schemas/RoastLevel'
        beans:
          type: string
          nullable: true
        public:
          type: boolean
          description: Whether this recipe is publicly visible
      additionalProperties: false

    CreateRecipeResponse:
      $ref: './models.yaml#/components/schemas/Recipe'

